apiVersion: v1
kind: Service
metadata:
  name: dgraph-http-service
  namespace: dgraph
  annotations:
    konghq.com/plugins: keycloak-jwt-plugin
spec:
  ports:
  - name: http-port    
    protocol: TCP
    port: 8080
    targetPort: 8080